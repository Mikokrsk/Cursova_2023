using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CombatController : MonoBehaviour
{
/*    [SerializeField] private PlayerStats _playerStats;

    void Start()
    {
        _playerStats = PlayerStats.Instance;
    }

    void Update()
    {
        if(Input.GetKeyDown(_playerStats.attack1KeyCode))
        {
            _playerStats.animController.Attack1();
        }
        if(Input.GetKeyDown(_playerStats.attack2KeyCode))
        {
            _playerStats.animController.Attack2();
        }
        if(Input.GetKeyDown(_playerStats.attack3KeyCode))
        {
            _playerStats.animController.Attack3();
        }
    }*/
}
